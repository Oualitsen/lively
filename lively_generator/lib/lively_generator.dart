/// Code generator for the lively reactive Flutter widget package.
///
/// Registers the `livelyBuilder` build factory used by `build_runner`.
/// Add this package as a `dev_dependency` and run `dart run build_runner build`.
library lively_generator;

export 'builder.dart';
